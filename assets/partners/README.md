# Partner logos

Place **official** partner logos here using the same filenames as in `index.html` (or update the `src` paths). Preferred: **SVG** or **PNG** on a transparent background, roughly **320×120 px** or similar aspect ratio.

Current files are **placeholders** generated for layout only—replace them before external publication if brand guidelines require it.
